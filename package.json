{
  "name": "hearth",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/bridge",
    "packages/agent",
    "packages/slack-bridge"
  ],
  "scripts": {
    "dev": "concurrently --names bridge,agent,slack --prefix-colors blue,green,magenta \"npm run dev -w packages/bridge\" \"npm run dev -w packages/agent\" \"npm run dev -w packages/slack-bridge\"",
    "build": "npm run build -w packages/bridge && npm run build -w packages/agent && npm run build -w packages/slack-bridge"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.7.0"
  }
}
